name: PR Title Check

on:
  pull_request:
    types: [opened, reopened, edited, labeled, unlabeled]

jobs:
  check-title:
    uses: QuickRelease/.github/.github/workflows/pr-title.yml@main
    secrets: inherit
